# Minimal install for running the unit test suite locally.
# Heavy ML deps (sentence-transformers/torch) are NOT required —
# all tests that touch embeddings/reranking mock those calls.
#
# Usage:
#   pip install -r requirements-test.txt
#   pytest tests/unit -v

# Core app deps needed for imports to resolve
fastapi>=0.115.0
python-multipart>=0.0.9
pydantic>=2.8.0
pydantic-settings>=2.4.0
sqlalchemy>=2.0.35
asyncpg>=0.29.0
httpx>=0.27.0
structlog>=24.4.0
tenacity>=9.0.0

# Async SQLite for repository/indexer tests (no Postgres needed)
aiosqlite>=0.20.0

# Parsers exercised directly in test_ingestion_pipeline.py
beautifulsoup4>=4.12.0
openpyxl>=3.1.0
python-docx>=1.1.0
python-pptx>=1.0.0
pymupdf>=1.24.0

# LLM SDKs — only needed so imports resolve; calls are mocked in tests
openai>=1.50.0
anthropic>=0.34.0

# Token counting used by chunkers (falls back gracefully if absent, but needed for exact-count tests)
tiktoken>=0.7.0

# MinIO SDK — client is mocked in tests, but the import must resolve
minio>=7.2.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
